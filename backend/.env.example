# 复制为 .env 并按需修改。真实值不入库。

# 本地开发：Supabase CLI 本地栈（`supabase start` 后固定是这个地址）。
# 这个变量是**必需**的 —— app/db.py 不再提供 localhost 兜底默认值，
# 缺了会启动即失败并提示你复制本文件。理由见 db.py 的 resolve_database_url：
# 有兜底的话，生产上忘配变量会表现为"进程健康、每个请求 500"，极难排查。
DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:54322/postgres

# 生产（配在 Render 控制台，不是这里）：
# 用 Supabase 的 **connection pooler / session 模式（5432）** 连接串，
# 不要用 direct connection —— 直连是 IPv6-only，Render 出站未必支持。
# 从控制台复制过来的串是 postgresql:// 开头，直接粘贴即可：
# app/db.py 会把它归一化到 psycopg v3 驱动。

# 只有自家 Next.js 才知道的共享 secret，通过 X-Backend-Secret 请求头传递。
# **必需** —— 不设的话所有请求一律 401（fail-closed）。方向是刻意的：
# 若表现为放行，后端就对全互联网敞开，而且没有任何外部征兆。
# 生产值配在 Render 控制台。必须与前端的 SITE_PASSWORD 是不同的值。
BACKEND_SECRET=local-dev-secret-not-a-real-one

# API 自动文档（/docs、/openapi.json）。**默认关闭**，设了这个变量才开。
# 写成"显式开启"而不是"判断是不是生产就关"，是因为环境判断失误的后果不对称：
# 前者失误只是本地少了文档，后者失误会把字段名（wechat/email/nick）公开出去，
# 等于宣告这里存了哪类个人信息。
# **生产环境不要设这个变量。**
# 刻意保持注释状态：连模板里都不预先启用，这样即便有人把本文件整段贴进
# 平台的环境变量，默认得到的仍是"文档关闭"。本地要用就取消注释。
# ENABLE_API_DOCS=1
