# 复制为 .env 并按需修改。真实值不入库。

# 本地开发：Supabase CLI 本地栈（`supabase start` 后固定是这个地址）。
# 不设此变量时 app/db.py 也会退回到同样的默认值。
DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:54322/postgres

# 生产（配在 Render 控制台，不是这里）：
# 用 Supabase 的 **connection pooler / session 模式（5432）** 连接串，
# 不要用 direct connection —— 直连是 IPv6-only，Render 出站未必支持。
# 从控制台复制过来的串是 postgresql:// 开头，直接粘贴即可：
# app/db.py 会把它归一化到 psycopg v3 驱动。
