# 复制为 .env.local 并按需修改。真实值不入库。

# FastAPI 后端地址。**server-side only** —— 只在 Server Component /
# lib/api.ts 里读取，浏览器拿不到。
#
# 绝不要加 NEXT_PUBLIC_ 前缀：那会把后端地址打进客户端 bundle，
# 违反"浏览器只与 Next.js 通信"的架构纪律（见 CLAUDE.md）。
#
# 本地：
BACKEND_URL=http://127.0.0.1:8000

# 生产（配在 Vercel 控制台，不是这里）：Render 分配的服务地址，
# 形如 https://<service-name>.onrender.com

# 整站共享密码（HTTP Basic Auth）。**必需** —— 不设的话 proxy.ts 会拒绝
# 所有请求（fail-closed）：进不去，而不是谁都能进。这个方向是刻意的，
# 因为"忘配变量"若表现为放行，从外面看不出任何异常，可能几个月无人察觉。
#
# 生产环境的真实密码配在 Vercel 控制台，绝不写进仓库。密码强度是唯一防线
# ——本项目不做限流，短口令等于没有保护。
SITE_PASSWORD=local-dev-password-not-a-real-one

# 调用 FastAPI 用的共享 secret，与上面的密码是**两个不同的值**：
# 一个给人输，一个机器用，用途与轮换节奏都不同。
#
# **server-side only** —— 绝不要加 NEXT_PUBLIC_ 前缀，那会把 secret 编译进
# 客户端 bundle 发给每个访客。openspec/config.yaml 的泄露检查会扫这一点。
BACKEND_SECRET=local-dev-secret-not-a-real-one
